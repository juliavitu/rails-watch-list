<h1>Lists#index</h1>
<div class="container">
  <ul class="list-group">
  <% @lists.each do |list| %>
  <li class="list-group-item"> <%= link_to list.name, list_path(list) %></li>
  <% end %>
  <%= link_to "Create a list", new_list_path %>

  </ul>
</div>
